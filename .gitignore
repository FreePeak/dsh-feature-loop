node_modules/
lib/
dist/
*.tsbuildinfo

# The loop's own run history (`runs.jsonl`), written under the invoking
# directory. Local state, not source — the derived ceilings are computed from it.
.feature-loop/

# Created by installing dependencies for this feature.
pnpm-lock.yaml
